import React from "react";
import {gql, useMutation} from "@apollo/client"

export default function RefuseContact() {

	const REFUSE_CONTACT = gql`mutation RefuseRequestContact($input: number){
		
	}`

}